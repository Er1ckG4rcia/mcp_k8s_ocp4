apiVersion: v1
kind: ServiceAccount
metadata:
  name: mcp-ocp-observer-sa
  namespace: validacao-infra
  labels:
    app: mcp-ocp-observer
    app.kubernetes.io/name: mcp-ocp-observer
    app.kubernetes.io/component: mcp-server
    app.kubernetes.io/part-of: mcp-platform
    app.kubernetes.io/version: "1.0.0"
  annotations:
    # This ServiceAccount is bound to mcp-ocp-observer-clusterrole (read-only).
    # It MUST NOT be used for any write operations.
    description: "Service account for mcp-ocp-observer — read-only cluster access"
